Short Project 11: Divide and conquer, Enumeration

Individual tasks (optional): 
2. Implemented the following methods in Enumerate.java:
   combine(): count/enumerate combinations,
   heap(): Heap's algorithm for permutations.

Leejia James

How to Compile:
1. Unzip contents of lxj171130.zip into a folder named "lxj171130".
2. Run the command `javac lxj171130/*.java` to compile.
3. Run the command `java lxj171130.Enumerate <n> <k>` to execute Enumerate.